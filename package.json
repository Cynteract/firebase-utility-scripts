{
  "dependencies": {
    "commander": "^14.0.0",
    "firebase-admin": "^13.2.0",
    "firestore-backfire": "^2.6.0",
    "nodemailer": "^7.0.12"
  },
  "scripts": {
    "backup:dev": "backfire export ./before_migration -P cynteract-test --adc --overwrite",
    "compare:dev": "node compareFirestore.js --dev --overwrite",
    "restore:dev": "backfire import ./before_migration -P cynteract-test --adc --mode=overwrite",
    "backup:prod": "backfire export ./before_migration -P cynteract-a52e4 --adc --overwrite",
    "compare:prod": "node compareFirestore.js --prod --overwrite",
    "restore:prod": "backfire import ./before_migration -P cynteract-a52e4 --adc --mode=overwrite"
  }
}